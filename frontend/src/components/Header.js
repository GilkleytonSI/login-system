function Header() {
    return (
        <nav className="navbar navbar-dark bg-primary px-3">
            <span className="navbar-brand">Painel de Controle</span>
        </nav>
    );
}

export default Header;
